{
  "name": "tinder-music",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "request": "^2.88.2"
  },
  "scripts": {
    "start": "node server/server.js",
    "build": "cd client && npm i && npm run build"
  }
}
